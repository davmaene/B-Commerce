import * as React from 'react';
import { View, Text } from 'react-native';

export const PanierScreen = ({ navigation, route }) => {
    return(
        <View>
            <Text>Panier</Text>
        </View>
    )
}